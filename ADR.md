# ADR-001：统一异步 Job 与产物引用

- 状态：已接受，待 A/B 配对组共同签字
- 日期：2026-09-17

## Context

DRAFT、全量检测、增量检测和修复都可能超过一次 HTTP 请求的生命周期。任务之间还需要交换镜像、日志、依赖图、错误报告和补丁，并保证仓库提交与构建配置一致。

## Decision

1. 四个服务使用同一 Job 外层结构和状态枚举。
2. 创建任务使用 POST，立即返回 HTTP 202、服务端 `job_id` 和 `QUEUED`。
3. 客户端通过 `GET /v1/jobs/{job_id}` 查询状态和产物引用。
4. 大文件使用 artifact URI 传递，引用记录生产任务、commit、媒体类型和 SHA-256；依赖图额外记录 `configuration_id`。
5. `MISSING`/`REDUNDANT` 是成功分析的发现，写入报告；环境、超时和分析器异常才写入 `job.error`。
6. MDFixer 只处理 `MISSING`，且必须完成干净构建、测试和重检后才能接受补丁。

## Alternatives

- 同步等待：实现简单，但客户端与执行器耦合，容易超过 HTTP 超时，拒绝。
- 在状态响应中内嵌全部文件：读取方便，但响应过大且难以校验复用，拒绝。
- 每个服务使用不同任务模型：局部自由度高，但联调和追踪复杂，拒绝。

## Consequences

服务需要保存任务状态与产物元数据，消费者需要轮询并解析 URI。好处是四个阶段可由同一 `trace_id` 追踪，文件可以独立下载和校验。Schema 的破坏性变更必须提升主版本并由 A/B 双方同步。

## 验证

运行 `python scripts/validate.py`。四类有效请求应通过，非法 `job_type` 和缺失 baseline 的增量请求应被拒绝；正常 MD 报告不得被识别为系统错误。

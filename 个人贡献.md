# B05组个人贡献记录

## 项目边界说明

- 实验仓库：本目录中的E2契约材料，包含 `contracts/`、`scripts/`、接口文档和验证记录。
- 样例项目： [RIKUHONG/cJSON](https://github.com/RIKUHONG/cJSON) 仅用于提供仓库地址、提交版本、构建命令和依赖分析场景。
- A05组配对仓库： [liyuhan7/devops-build-toolchain](https://github.com/liyuhan7/devops-build-toolchain)。

## B05组成员分工

| 成员                    | 负责方向 | 具体工作 | 主要产出与验证证据 |
|-----------------------| --- | --- | --- |
| B05-01（陆泓/241250105）  | 统一任务模型与接口架构 | 负责DRAFT、FULL_CHECK、INCREMENTAL_CHECK、REPAIR四类异步Job的公共字段、状态机、端点和错误处理设计；整理A/B接口差异并负责版本迁移决策。 | `contracts/common/task.schema.json`、`contracts/common/job.schema.json`、`contracts/interface-index.json`、`ADR.md`、`MIGRATION.md`；验证Job状态、终态和错误码规则。 |
| B05-02（刘君杰/241250049） | DRAFT与构建环境契约 | 负责DRAFT请求/响应模型、`input.limits`、Dockerfile和镜像产物、迭代日志、构建/验证成功判据；将旧版扁平字段迁移为A组现行结构。 | `contracts/draft/` 全部样例、DRAFT相关Schema定义、迭代日志和失败终态样例；验证干净构建、验证命令和每轮变更理由。 |
| B05-03（刘洋/241250037）  | MDFixer与Artifact交接 | 负责REPAIR的MISSING-only边界、完整 `error_report` 引用、候选补丁、构建/测试/重检结果，以及Artifact URI、`repository_commit`、`configuration_id`和SHA-256约束。 | `contracts/mdfixer/`、`contracts/common/artifact.schema.json`、`contracts/common/finding.schema.json`、Artifact说明；验证被拒候选必须记录阶段和原因，接受补丁前重检必须清零。 |
| B05-04（李新昊/241250002） | Schema校验、负向样例与文档 | 负责校验器、四类请求/响应样例、非法输入样例、接口说明和验证报告；执行A组版本同步后的回归检查，确认旧字段不会混入现行契约。 | `scripts/validate.py`、`contracts/negative/`、`documents/interfaces/`、`validation-results.md`、`CHANGELOG.md`；最终结果为 `17 passed, 0 failed`。 |

## 共同完成的工作

- 将本组契约同步到A组当前版本，公共Schema和任务样例文件集合已对齐。
- 统一字段命名：`input.limits`、`environment.image_uri`、`error_report`、`repository_commit`、`CONTAINER_IMAGE`。
- 补充失败、拒绝候选和非法请求样例，并用Schema驱动校验器验证跨字段一致性。
- 明确样例项目与实验仓库的边界，避免把cJSON误写为本实验代码仓库。

## 验证结果

在实验仓库 `E2/` 目录执行：

```bash
python scripts/validate.py
```

结果：

```text
17 passed, 0 failed
```

## 提交记录

| 提交SHA | 作者 | 提交说明 | Issue/PR |
| --- | --- | --- | --- |
| 待填写 | B05全体 | `docs: synchronize E2 contracts with A group` | 待填写 |

## 待补充信息
- 实验仓库的最终提交SHA、Issue/PR和双方真实Artifact互读记录。
